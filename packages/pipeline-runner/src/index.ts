// Pipeline runner — barrel export (populated as modules are added)
export type {
  PipelineSpec,
  StageSpec,
  TerminationPolicy,
  StopConditionRef,
  ReducerRef,
  ErrorPolicy,
  SchemaRef,
  PipelineContext,
} from './spec.js';
