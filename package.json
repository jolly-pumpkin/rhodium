{
  "name": "rhodium-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --cwd packages/capabilities build && bun run --cwd packages/core build && bun run --cwd packages/graph build && bun run --cwd packages/testing build && bun run --cwd packages/rhodium build",
    "test": "bun --filter '*' test",
    "typecheck": "bun --filter '*' run typecheck"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11",
    "typescript": "^5.4.0"
  }
}
